import { View, Text } from 'react-native'; // to view, and view the text prob?
import "expo-router/entry"; // what is this?


const Home = () => { //why -- its the Home function that will view a text "Home". At the end u export it

return <View>
    <Text>Je</Text>
</View>
}

export default Home;